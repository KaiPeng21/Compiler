PROGRAM testf9
BEGIN
	STRING space := " ";
	STRING eol := "\n";

	FUNCTION INT MULT (INT a, INT b)
	BEGIN
		INT ret1;
		ret1 := a * b;
		RETURN ret1;
	END
	
	FUNCTION INT INCT (INT i)
	BEGIN
		RETURN i + 1;
	END
	
	FUNCTION INT main ()
	BEGIN
		INT i, a, b, c;	
		FOR (i := 0; i < 15; i := i + 2)
			WRITE(i, space);
			IF (i < 6)
				INT j;
				j := 1;
				WRITE(j, eol);
			ELSE
				INT j;
				j := 5;
				WRITE(j, eol);
			FI
		ROF	
		RETURN 0;
	END

END	
